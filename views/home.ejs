
    <%- include('include/header'); -%>
    <h1>Home</h1>
    <P> <%- startingContent %> </P>
     
      <% posts.forEach( post => {%>
        
      <h1> <%= post.title %> </h1>
      
        <%let length = 100; %>
        <% let string = post.content %>
        <%  let myTruncatedString = string.substring(0, length);%>
      <p> <%= myTruncatedString %> </p>

      <a href="/posts/ <%= post.title %> ">read more</a>

      <% }) %>

         <%- include("include/footer"); %>

